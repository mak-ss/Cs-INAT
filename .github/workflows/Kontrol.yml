name: Domain Değişiklik Kontrolü

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  schedule:
    - cron: '25 */6 * * *'

jobs:
  DomainKontrol:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Depo Kontrolü
        uses: actions/checkout@v4
        with:
          # Eğer PAT kullanacaksan gizli olduğundan emin ol, yoksa varsayılan GITHUB_TOKEN yeterlidir
          token: ${{ secrets.PAT_TOKEN || github.token }}

      - name: Python Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Gereksinimleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install -U cloudscraper pycryptodome

      - name: Betiği Çalıştır
        run: python KONTROL.py

      - name: Değişiklik Kontrolü ve Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          git add -A
          
          # Değişiklik var mı kontrol et (staging area üzerinde)
          if ! git diff --cached --exit-code; then
            echo "Değişiklik tespit edildi, commit yapılıyor..."
            git commit -m "🤖 Domain Update" -m "🤖 Automatic domain updates have been applied."
            
            # Dal ismini otomatik tespit edip push et (master veya main fark etmez)
            git push origin HEAD
          else
            echo "Herhangi bir değişiklik yok."
          fi
